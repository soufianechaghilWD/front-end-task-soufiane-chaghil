import cart_action from "./cart_action";
import currency_action from "./currency_action";
import header_active_item_action from "./header_active_item_action";

const all_actions = {
  cart_action,
  currency_action,
  header_active_item_action,
};

export default all_actions;
